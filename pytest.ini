[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: fast deterministic unit tests
    integration: tests requiring external services or network access
    llm: tests requiring DashScope or another LLM provider
    external_api: tests requiring live finance/data APIs
