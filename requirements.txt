# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
gradio>=4.0.0
pydantic>=2.0.0
requests>=2.31.0

# DashScope LLM API
dashscope>=1.14.0

# LangGraph for workflow orchestration
langgraph>=1.0.0
langchain>=1.0.0
langchain-core>=1.0.0

# Redis for caching
redis>=5.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0
email-validator>=2.0.0
apscheduler>=3.10.0

# v2 (DeepSeek Harness)
pydantic-settings>=2.0
fastmcp>=2.0
deepseek-harness-sdk==0.1.1rc1  # 需 --pre；runtime-bin 无 mac-x64 轮子时走 npm 模式(DSH_RUNTIME=npm)；mac-x64 需 pip install --no-deps

# Optional: curl-cffi>=0.7 —— TLS 指纹反爬兜底(东财渠道)；缺失时自动降级 requests
# curl-cffi>=0.7
